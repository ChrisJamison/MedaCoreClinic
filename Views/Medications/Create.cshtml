@model WebApplication1.Models.Medication

@{
    Layout = "~/Views/Shared/_MedaCoreLayout.cshtml";
    ViewData["Title"] = "Thêm mới thuốc";
    ViewData["Action"] = "Create"; // Action for the form submission
    ViewData["SubmitButtonText"] = "Thêm mới"; // Button text
}

<div class="container mt-4">
    <h2>@ViewData["Title"]</h2>
    @await Html.PartialAsync("_MedicationFormPartial", Model)
</div>