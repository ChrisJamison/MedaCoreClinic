
@model List<WebApplication1.Models.User>

@{
    Layout = "~/Views/Shared/_MedaCoreLayout.cshtml";
    ViewData["Title"] = "User Management";
    var currentPage = ViewBag.CurrentPage as int? ?? 1;
    var totalPages = ViewBag.TotalPages as int? ?? 1;
}

<h2 class="mb-4">User List</h2>

<div class="mb-3">
    <a href="/Users/Create" class="btn btn-success">
        <i class="bi bi-plus-circle"></i> Thêm mới
    </a>
</div>

<table class="table table-bordered table-hover">
    <thead class="table-light">
        <tr>
            <th scope="col">STT</th>
            <th scope="col">Tên tài khoản</th>
            <th scope="col">Địa chỉ Email</th>
            <th scope="col">Mật khẩu</th>
            <th scope="col">Số điện thoại</th>
            <th scope="col">Địa chỉ</th>
            <th scope="col">Ngày sinh</th>
            <th scope="col">Quyền hạn</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model)
        {
            <tr>
                <td>@user.Id</td>
                <td>@user.Username</td>
                <td>@user.Email</td>
                <td>@user.Password</td>
                <td>@user.PhoneNumber</td>
                <td>@user.Address</td>
                <td>@user.DateOfBirth</td>
                <td>@user.Role</td>
                <td>
                    <!-- Edit Action -->
                    <a href="/User/Edit/@user.Id" class="btn btn-outline-primary btn-sm">
                        <i class="bi bi-pencil"></i> Edit
                    </a>
                    <!-- Delete Action -->
                    <form asp-action="DeleteConfirmed" asp-controller="User" method="post" style="display:inline" onsubmit="return confirmDelete(event)">
                        <input type="hidden" name="id" value="@user.Id" />
                        <button type="submit" class="btn btn-outline-danger btn-sm">
                            <i class="bi bi-trash"></i> Delete
                        </button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>

<!-- Pagination -->
@if (totalPages > 1)
{
    <nav>
        <ul class="pagination">
            @if (currentPage > 1)
            {
                <li class="page-item">
                    <a class="page-link" href="/User?page=@(currentPage - 1)">Previous</a>
                </li>
            }

            @for (var pageA = 1; pageA <= totalPages; pageA++)
            {
                <li class="page-item @(pageA == currentPage ? "active" : "")">
                    <a class="page-link" href="@Url.Action("Index", "User", new { page = pageA })">@pageA</a>
                </li>
            }

            @if (currentPage < totalPages)
            {
                <li class="page-item">
                    <a class="page-link" href="@Url.Action("Index", "User", new { page = currentPage + 1 })">Next</a>
                </li>
            }
        </ul>
    </nav>
}

<script>
    function confirmDelete(event) {
        // Display a confirmation prompt
        const confirmation = confirm("Bạn có chắc chắn muốn xóa bệnh nhân này không?");
        if (!confirmation) {
            // If the user cancels, prevent the form submission
            event.preventDefault();
            return false;
        }
        return true; // Allow form submission for deletion
    }
</script>